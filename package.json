{
  "name": "maxi-q",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint": "biome check .",
    "check": "npm run check --workspaces --if-present",
    "format": "npm run fix --workspaces --if-present"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.3"
  }
}
